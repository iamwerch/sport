import { VoyConversionPipe } from './voy-conversion.pipe';

describe('VoyConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new VoyConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
